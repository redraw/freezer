---
label: Proyecto
hide_body: false
fields:
- type: file
  name: thumbnail
  label: Thumbnail
  description: This will only show up on the front page
- type: text
  name: title
  label: Title
  config:
    required: true
- type: datetime
  name: date
  label: Date
  config:
    required: true
    date_format: YYYY-MM-DD
    time_format: ''
    export_format: YYYY-MM-DD
    display_utc: true
  default: now
  hidden: true
- type: color
  name: bg_color
  config:
    color_format: Hex
  label: Background color
- type: color
  name: fg_color
  config:
    color_format: Hex
  label: Foreground color
- name: galeria
  type: field_group
  config: {}
  fields:
  - name: stack
    type: boolean
    label: stack
    default: true
  - name: autores
    type: field_group_list
    fields:
    - name: nombre
      type: text
      config:
        required: false
      label: nombre
    - name: url
      type: text
      config:
        required: false
      label: url
    - name: imagenes
      type: image_gallery
      config: {}
      label: imagenes
    config:
      min: 
      max: 
      labelField: 
    label: autores
  label: galeria
pages:
- content/proyectos/test.md
